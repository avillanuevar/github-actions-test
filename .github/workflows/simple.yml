name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: windows-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: multiline scripts
        run: |
          node -v
          npm -v
